/**
 * Minimal config used for the API Reference component.
 *
 * Where possible these properties should live in the workspace JSON
 * A workspace config will be provisioned to the workspace when it is created
 */
export type ScalarApiReferenceConfig = {}

/**
 * Workspace config for the API Reference component.
 *
 * These options will be merged into the workspace JSON store as extensions
 *
 */
export type ScalarWorkspaceConfig = {}
